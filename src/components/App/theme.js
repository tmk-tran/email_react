import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00A79D", // Replace with your desired primary color
    },
    secondary: {
      main: "#005296", // Replace with your desired secondary color
    },
  },
});

export default theme;
